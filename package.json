{
  "name": "tw-market-radar",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "check": "node --check app.js && node --check data.js && node --check live.js && node scripts/validate-data.mjs",
    "serve": "python3 -m http.server 8080"
  }
}
